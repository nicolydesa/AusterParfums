# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/auster_parfums"

# Random secret for JWT session cookies (min. 32 characters)
AUTH_SECRET="replace-with-a-long-random-string-at-least-32-chars"
